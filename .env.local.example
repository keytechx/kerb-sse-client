# SSE Server Configuration
# Copy this file to .env.local and fill in your values

# Backend SSE server URL
VITE_SSE_BASE_URL=http://localhost:8086

# API Key for authentication (get this from backend .env file)
VITE_API_KEY=your-api-key-here

# Session ID for SSE subscription (optional - will auto-generate if not provided)
# This is used as the reference ID for the SSE channel
VITE_SESSION_ID=session-test-123
